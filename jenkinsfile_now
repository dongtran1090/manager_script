pipeline {
    agent any

    triggers {
        cron('* * * * *')
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Run script') {
            steps {
                bat 'python script_now.py'
            }
        }
    }
}
