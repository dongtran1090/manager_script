pipeline {
    agent any

    triggers {
        cron('*/5 * * * *')
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Run script') {
            steps {
                bat 'python script_run_5m.py'
            }
        }
    }
}
